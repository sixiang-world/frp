[
  {
    "id": 1,
    "prompt": "在一台新 Linux VPS 上部署 frps，开启 Dashboard + Token 认证 + TLS 加密",
    "expected": "输出完整部署流程：下载命令 → 配置文件模板（含 auth.token + transport.tls.force + webServer）→ systemd 服务 → 启动验证 → Dashboard 访问方式"
  },
  {
    "id": 2,
    "prompt": "frpc 报错 'login to server failed: i/o timeout'，帮我排查原因并提供修复步骤",
    "expected": "定位防火墙/安全组问题 → 给出 nc -zv 测试命令 → 指导修改云服务商安全组规则 → 重试验证"
  },
  {
    "id": 3,
    "prompt": "我需要通过 frp 把内网的 Jupyter Notebook（8888）和 RDP（3389）同时暴露出去，写配置",
    "expected": "输出 frpc.toml 中两个 [[proxies]] 段（jupyter 和 rdp），含 remotePort、transport.useEncryption、localIP/localPort"
  },
  {
    "id": 4,
    "prompt": "frps Dashboard 绑定在 0.0.0.0 了，怎么修？还要把日志级别改成 debug",
    "expected": "指出反模式风险 → 修改 webServer.addr = \"127.0.0.1\" → SSH 隧道访问方案 → log.level = \"debug\" → 重启 frps"
  },
  {
    "id": 5,
    "prompt": "我想把两个内网机器的 SSH 通过同一个 frps 暴露，两个 frpc 用同样的代理名会冲突吗？",
    "expected": "说明 user 字段的命名空间隔离机制 → 给出两台 frpc 各自的配置示例（user = \"alpha\" / user = \"beta\"）→ Dashboard 查看方式"
  }
]
